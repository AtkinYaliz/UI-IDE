/**
 * Copyright (c) 2014-present, Facebook, Inc. All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 *
 * 
 */

'use strict';var _require =

require('./parsers/BabylonParser');const babylonParser = _require.babylonParser;var _require2 =
require('./parsers/ParserNodes.js');const ItBlock = _require2.ItBlock,Expect = _require2.Expect;






/**
                                                                                                  * Converts the file into an AST, then passes out a
                                                                                                  * collection of it and expects.
                                                                                                  */
function parse(file) {
  if (file.match(/\.tsx?$/)) {
    // This require is done here so that it can be optional for clients
    var _require3 = require('./parsers/TypeScriptParser');const parse = _require3.parse;
    return parse(file);
  } else {
    return babylonParser(file);
  }
}

module.exports = {
  parse };